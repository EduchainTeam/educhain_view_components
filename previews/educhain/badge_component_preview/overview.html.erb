<table>
  <tr>
    <td></td>
    <% component("badge").SIZES.keys.each do |size| %>
      <td class="px-3 py-1 text-gray-500 text-center"><%= size.to_s.humanize %></td>
    <% end %>
  </tr>
  <% component("badge").COLORS.keys.each do |color| %>
    <tr>
      <td class="font-bold px-3 py-1"><%= color.to_s.humanize %></td>
    </tr>
    <tr>
      <td class="text-gray-500 px-3 py-1">None</td>
      <% component("badge").SIZES.keys.each do |size| %>
        <td class="px-3 py-1">
          <%= render component("badge", name: name, color: color, size: size) %>
        </td>
      <% end %>
    </tr>
  <% end %>
</table>

<div class="mt-8">
  <h6 class="text-gray-500 mb-3 mt-0">
    Predefined values
  </h6>

  <% [component("badge").yes, component("badge").no].each do |component| %>
    <%= render component %>
  <% end %>
</div>