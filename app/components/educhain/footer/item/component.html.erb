<div class="<%= width %> flex flex-col gap-3">
  <div class="text-sm font-semibold"><%= key %></div>

  <div class="flex flex-col gap-2">
    <% links.each do |link| %>
      <div class="text-smaller"><%= link_to link.name, link.path %></div>
    <% end %>
  </div>
</div>
